import React from "react";
import ReactDOM from "react-dom/client";
import NexTalkApp from "./nextalk-app-v2";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NexTalkApp />
  </React.StrictMode>
);
